- name:configur mongodb
  hosts: mongodb
  become: true
  tasks:
    - name: copy mongodb repo file
      ansible.builtin.copy:
        src: mongo.repo
        dest: /etc/yum.repos.d/mongo.repo
    - name: install mongodb
      ansible.builtin.dnf:
        name: mongodb-org
        state: present